apiVersion: v1
kind: Service
metadata:
  name: { { include "website-template.fullname" . } }
  labels: { { - include "website-template.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "website-template.selectorLabels" . | nindent 4 } }
